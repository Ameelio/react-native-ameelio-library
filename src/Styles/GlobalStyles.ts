import { ViewStyle } from 'react-native';

export const shadow: ViewStyle = {
  shadowColor: '#000',
  shadowOffset: {
    width: 0,
    height: 2,
  },
  shadowOpacity: 0.23,
  shadowRadius: 2.62,
  elevation: 4,
};

export const rounded: ViewStyle = {
  borderRadius: 8,
};
